def return_every_kind(a: Any, b) -> Union[
    Dict[List[str, str]],
    Dict[{str:str}],
    Dict[{}],
    List[number, number],
    Tuple[str, str],
    complex(number, number),
    float(number),
    int(number),
    long(number),
    number,
]: ...
